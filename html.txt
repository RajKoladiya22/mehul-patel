<!doctype html>
<html lang="en">

<head>
    <!-- Basic -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=5, user-scalable=yes">
    <title>MEHUL PATEL</title>
    <meta name="description" content="MEHUL PATEL">
    <link rel="canonical" href="https://mehul-patel.vercel.app/">
    <meta name="robots" content="index,follow">
    <link rel="icon" href="/images/favicon.ico">

    <!-- Open Graph / Facebook -->
    <meta property="og:locale" content="en_US">
    <meta property="og:type" content="profile">
    <meta property="og:title" content="MEHUL PATEL">
    <meta property="og:description" content="MEHUL PATEL">
    <meta property="og:url" content="https://mehul-patel.vercel.app/">
    <meta property="og:site_name" content="Shivansh Infosys / MagicallySoft">
    <meta property="og:image" content="/images/s-logo.pngf">
    <meta property="og:image:secure_url" content="/images/s-logo.png">
    <meta property="og:image:type" content="image/png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:image:alt" content="Mehul Patel">

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="@YourTwitterHandle">
    <meta name="twitter:creator" content="@YourTwitterHandle">
    <meta name="twitter:title" content="MEHUL PATEL">
    <meta name="twitter:description" content="MEHUL PATEL">
    <meta name="twitter:image" content="/images/s-logo.pngf">
    <meta name="theme-color" content="#0f3d3e">

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Person",
      "name": "Mehul Patel",
      "alternateName": "MEHUL PATEL",
      "jobTitle": "Professional ",
      "worksFor": {
        "@type": "Organization",
        "name": "Abc Salon"
      },
      "image": "/images/s-logo.png",
      "url": "https://mehul-patel.vercel.app/",
      "sameAs": [
        "https://bitly.cx/rNEH4",
        "https://www.facebook.com/profile.php?id=61573592214242&mibextid=rS40aB7S9Ucbxw6v",
        "https://wa.me/8141703007",
        "https://api.whatsapp.com/send/?phone=8141703007&text&type=phone_number&app_absent=0",
        "https://www.linkedin.com/in/mehul-patel-0a7973355?utm_source=share&utm_campaign=share_via&utm_content=profile&utm_medium=android_app",
        "https://www.instagram.com/shivansh_infosys?igsh=MW5vdjB5YTY2Y2pmdw=="
      ],
      "description": "MEHUL PATEL"
    }
    </script>

    <!-- Preconnect to external domains -->
    <link href="https://fonts.googleapis.com/css2?family=Sofia+Sans+Semi+Condensed:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link rel="preconnect" href="https://cdnjs.cloudflare.com">

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <!-- Font Awesome for icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet"
        integrity="sha512-Avb2QiuDEEvB4bZJYdft2mNjVShBftLdPG8FJ0V7irTLQ8Uo0qcPxh4Plq7G5tGm0rU+1SPhVotteLpBERwTkw=="
        crossorigin="anonymous">

    <!-- Fonts - optimized for mobile -->
    <link
        href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Inter:wght@300;400;500;600&display=swap"
        rel="stylesheet">

    <style>
        :root {
            --teal-primary: #000000;
            --teal-secondary: #4a0000;
            --teal-gradient: linear-gradient(180deg, #000000 0%, #C50202 90%, #000000 100%);
            --gold-primary: #caa45f;
            --gold-light: #d4b679;
            --text-primary: #e6efee;
            --text-secondary: #bcd0cf;
            --text-muted: #b7d0cf;
            --card-bg: #f7f7f7;
            --glass-bg: rgba(255, 255, 255, 0.04);
            --shadow-primary: 0 8px 32px rgba(0, 0, 0, 0.35);
            --shadow-card: 0 4px 20px rgba(0, 0, 0, 0.15);
            --border-radius: 12px;
            --border-radius-lg: 20px;
            --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* Performance optimizations */
        *,
        *::before,
        *::after {
            box-sizing: border-box;
        }

        a {
            text-decoration: none;
        }

        /* Base styles - Mobile first */
        html {
            scroll-behavior: smooth;
            -webkit-text-size-adjust: 100%;
        }

        body {
            font-family: "Sofia Sans Semi Condensed", "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: var(--teal-gradient);
            color: var(--text-primary);
            line-height: 1.6;
            margin: 0;
            padding: 0;
            /*min-height: 100vh;*/
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
            overflow-x: hidden;
        }

        /* Improved mobile hero section */
        .hero-wrap {
            position: relative;
            display: flex;
            flex-direction: column;
            justify-content: center;
            /*padding-top: 1.5rem;*/
        }

        .hero-bg {
            position: relative;
            width: 100%;
            height: 170px;
            background: linear-gradient(135deg,
                    rgba(255, 255, 255, 0.08) 0%,
                    rgba(202, 164, 95, 0.12) 50%,
                    transparent 100%);
            overflow: hidden;
        }

        .hero-bg .wave {
            position: absolute;
            bottom: -1px;
            left: 0;
            width: 100%;
            height: auto;
            z-index: 2;
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: url('images/s-logo.png');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center center;
            z-index: 1;
        }

        .hero-bg::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(ellipse 800px 200px at 50% 0%,
                    rgba(255, 255, 255, 0.1),
                    transparent 70%);
            z-index: 1;
        }

        /* Mobile-optimized profile photo */
        .profile-photo {
            position: relative;
            width: 130px;
            height: 130px;
            margin: -55px auto 0;
            border-radius: 50%;
            border: 4px solid var(--gold-primary);
            background: #ffffff;
            overflow: hidden;
            box-shadow: var(--shadow-primary);
            z-index: 10;
            transition: var(--transition);
        }

        .profile-photo:hover {
            transform: scale(1.05);
            box-shadow: 0 12px 40px rgba(202, 164, 95, 0.3);
        }

        .profile-photo img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition);
            filter: grayscale(5%) brightness(1.05);
        }

        /* Mobile-optimized intro section */
        .intro {
            text-align: center;
            padding: 1.5rem 1rem 1rem;
            position: relative;
            z-index: 5;
        }

        .intro h1 {
            font-family: "Playfair Display", serif;
            color: var(--gold-primary);
            font-size: 2.2rem;
            font-weight: 700;
            margin: 0 0 0.5rem;
            letter-spacing: 0.5px;
            line-height: 1.2;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        }

        .intro .subtitle {
            color: var(--text-primary);
            font-size: 1.1rem;
            margin-bottom: 1rem;
            font-weight: 500;
            opacity: 0.9;
        }

        .intro .description {
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.6;
            max-width: 350px;
            margin: 0 auto 1.5rem;
            font-weight: 300;
        }

        .company-links {
            display: flex;
            justify-content: center;
            gap: 1rem;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
        }

        .company-link {
            display: inline-flex;
            align-items: center;
            padding: 0.6rem 1.2rem;
            background: rgba(0, 0, 0, 0.2);
            color: var(--gold-primary);
            text-decoration: none;
            border-radius: var(--border-radius);
            font-size: 0.95rem;
            transition: var(--transition);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            box-shadow: var(--shadow-card);
        }

        .company-link:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.25);
        }

        /* Mobile-optimized social icons */
        .social-row {
            display: flex;
            gap: 14px;
            justify-content: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .social-btn {
            width: 52px;
            height: 52px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--border-radius);
            background: rgba(0, 0, 0, 0.2);
            color: var(--gold-primary);
            text-decoration: none;
            font-size: 1.2rem;
            transition: var(--transition);
            box-shadow: var(--shadow-card);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }

        .social-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), transparent);
            opacity: 0;
            transition: var(--transition);
        }

        .social-btn:hover,
        .social-btn:focus {
           /* transform: translateY(-4px) scale(1.05);*/
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
            box-shadow: var(--shadow-primary);
        }

        .social-btn:hover::before {
            opacity: 1;
        }

        .social-btn:active {
            transform: translateY(-2px) scale(1.02);
        }

        /* Contact section - mobile optimized */
        .contact-section {
            background: var(--teal-secondary);
            padding: 2.5rem 0;
            position: relative;
        }

        .section-title {
            font-family: "Playfair Display", serif;
            color: #fff;
            font-size: 1.8rem;
            text-align: center;
            margin-bottom: 2rem;
            font-weight: 600;
            position: relative;
            display: inline-block;
            left: 50%;
            transform: translateX(-50%);
            padding-bottom: 0.5rem;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 25%;
            width: 50%;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--gold-primary), transparent);
            border-radius: 3px;
        }

        .info-card {
            display: flex;
            align-items: center;
            gap: 1rem;
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.05) 0%, rgba(0, 0, 0, 0.05) 100%);
            padding: 1.2rem;
            border-radius: var(--border-radius);
            margin-bottom: 1rem;
            transition: var(--transition);
            box-shadow: var(--shadow-card);
            border: 1px solid rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            height: 100%;
        }

        .info-card:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-primary);
            background: linear-gradient(135deg, rgba(255, 255, 255, 0.08) 0%, rgba(0, 0, 0, 0.02) 100%);
            border-color: rgba(202, 164, 95, 0.2);
        }

        .info-icon {
            width: 55px;
            height: 55px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: var(--border-radius);
            background: rgba(0, 0, 0, 0.2);
            color: var(--gold-primary);
            font-size: 1.3rem;
            flex-shrink: 0;
            box-shadow: inset 0 1px 3px rgba(255, 255, 255, 0.1);
            transition: var(--transition);
        }

        .info-card:hover .info-icon {
            transform: scale(1.1);
            background: rgba(202, 164, 95, 0.2);
            color: #fff;
        }

        .info-text {
            flex: 1;
            min-width: 0;
        }

        .info-text small {
            color: var(--text-muted);
            font-size: 0.8rem;
            display: block;
            margin-bottom: 0.3rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: 500;
        }

        .info-text .value {
            font-size: 1rem;
            color: var(--text-primary);
            font-weight: 500;
            word-break: break-word;
        }

        /* Map container */
        .map-container {
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--shadow-card);
            transition: var(--transition);
            height: 150px;
        }

        .map-container:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-primary);
        }

        /* Services section - mobile first */
        .services-wrap {
            background: var(--card-bg);
            color: var(--teal-primary);
            border-top-left-radius: var(--border-radius-lg);
            border-top-right-radius: var(--border-radius-lg);
            padding: 2.5rem 0;
            margin-top: -1rem;
            position: relative;
        }

        .services-title {
            text-align: center;
            font-family: "Playfair Display", serif;
            color: var(--teal-primary);
            font-size: 1.8rem;
            margin-bottom: 2rem;
            font-weight: 600;
            position: relative;
            display: inline-block;
            left: 50%;
            transform: translateX(-50%);
            padding-bottom: 0.5rem;
        }

        .services-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 25%;
            width: 50%;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--gold-primary), transparent);
            border-radius: 3px;
        }

        .services-scroll-container {
            position: relative;
            width: 100%;
            overflow: hidden;
            padding: 0.5rem 0 1.5rem;
        }

        .services-scroll {
            display: flex;
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            gap: 1.2rem;
            padding: 0.5rem 1.2rem;
            scrollbar-width: none;
            scroll-behavior: smooth;
            -ms-overflow-style: none;
        }

        .services-scroll::-webkit-scrollbar {
            display: none;
        }

        .service-card {
            flex: 0 0 85%;
            scroll-snap-align: start;
            background: #fff;
            border: 2px solid var(--gold-light);
            border-radius: var(--border-radius);
            padding: 1.8rem 1.2rem;
            text-align: center;
            transition: var(--transition);
            box-shadow: var(--shadow-card);
            position: relative;
            overflow: hidden;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 5px;
            background: linear-gradient(90deg, var(--gold-primary), var(--gold-light));
            transition: var(--transition);
        }

        .service-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-primary);
            border-color: var(--gold-primary);
        }

        .service-card:hover::before {
            height: 6px;
            background: linear-gradient(90deg, var(--gold-light), var(--gold-primary));
        }

        .service-icon {
            width: 75px;
            height: 75px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.2rem;
            border-radius: var(--border-radius);
            background: linear-gradient(135deg, var(--gold-primary), var(--gold-light));
            color: #fff;
            font-size: 1.8rem;
            box-shadow: var(--shadow-card);
            transition: var(--transition);
        }

        .service-card:hover .service-icon {
            transform: scale(1.1) rotate(5deg);
            box-shadow: 0 8px 25px rgba(202, 164, 95, 0.4);
        }

        .service-card h5 {
            font-family: "Playfair Display", serif;
            color: var(--teal-primary);
            font-size: 1.3rem;
            margin-bottom: 0.8rem;
            font-weight: 600;
        }

        .service-card p {
            color: #666;
            font-size: 0.95rem;
            line-height: 1.6;
            margin: 0;
        }

        .scroll-indicator {
            display: flex;
            justify-content: center;
            gap: 0.6rem;
            margin-top: 1rem;
        }

        .scroll-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
            background: rgba(15, 61, 62, 0.3);
            transition: var(--transition);
            cursor: pointer;
        }

        .scroll-dot.active {
            background: var(--teal-primary);
            transform: scale(1.3);
        }

        /* Appointment section - mobile optimized */
        .appointment-wrap {
            background: var(--teal-primary);
            padding: 2.5rem 1rem;
            text-align: center;
        }

        .appointment-title {
            font-family: "Playfair Display", serif;
            color: var(--text-primary);
            font-size: 1.8rem;
            margin-bottom: 1.8rem;
            font-weight: 600;
            position: relative;
            display: inline-block;
            padding-bottom: 0.5rem;
        }

        .appointment-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 25%;
            width: 50%;
            height: 3px;
            background: linear-gradient(90deg, transparent, var(--gold-primary), transparent);
            border-radius: 3px;
        }

        .date-input-container {
            max-width: 400px;
            margin: 0 auto;
        }

        .date-input-label {
            display: block;
            color: var(--text-secondary);
            margin-bottom: 0.8rem;
            font-size: 0.95rem;
            text-align: left;
        }

        .date-input {
            width: 100%;
            padding: 1rem 1.2rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: var(--border-radius);
            background: rgba(255, 255, 255, 0.05);
            color: var(--text-primary);
            font-size: 1rem;
            transition: var(--transition);
            backdrop-filter: blur(10px);
            box-shadow: inset 0 2px 8px rgba(0, 0, 0, 0.2);
        }

        .date-input:focus {
            outline: none;
           /* border-color: var(--gold-primary);*/
            box-shadow: 0 0 0 3px rgba(202, 164, 95, 0.3);
            background: rgba(255, 255, 255, 0.08);
        }

        .book-btn {
            margin-top: 1.5rem;
            padding: 1rem 2.2rem;
            background: var(--gold-primary);
            color: var(--teal-primary);
            border: none;
            border-radius: var(--border-radius);
            font-size: 1.05rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: var(--transition);
            cursor: pointer;
            box-shadow: var(--shadow-card);
            min-width: 220px;
            position: relative;
            overflow: hidden;
        }

        .book-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: 0.5s;
        }

        .book-btn:hover,
        .book-btn:focus {
            background: var(--gold-light);
            transform: translateY(-3px);
            box-shadow: var(--shadow-primary);
        }

        .book-btn:hover::before {
            left: 100%;
        }

        .book-btn:active {
            transform: translateY(0);
        }

        .book-btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        /* Footer */
        .footer {
            background: var(--teal-primary);
            padding: 1.5rem 0;
            text-align: center;
            color: var(--text-secondary);
            font-size: 0.9rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .footer p {
            margin: 0.3rem 0;
        }

        /* Loading states */
        .loading {
            opacity: 0.7;
            pointer-events: none;
        }

        /* Accessibility improvements */
        .visually-hidden {
            position: absolute !important;
            width: 1px !important;
            height: 1px !important;
            padding: 0 !important;
            margin: -1px !important;
            overflow: hidden !important;
            clip: rect(0, 0, 0, 0) !important;
            white-space: nowrap !important;
            border: 0 !important;
        }

        /* Focus styles */
        *:focus {
            /*outline: 2px solid var(--gold-primary);*/
           /* outline-offset: 2px;*/
        }

        /* Animation keyframes */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }

        .animate-fade-in-up {
            animation: fadeInUp 0.6s ease-out forwards;
        }

        .animate-pulse {
            animation: pulse 2s infinite ease-in-out;
        }

        /* Tablet optimizations */
        @media (min-width: 576px) {
            .hero-bg {
                height: 240px;
            }

            .profile-photo {
                width: 150px;
                height: 150px;
                margin-top: -70px;
            }

            .intro h1 {
                font-size: 2.8rem;
            }

            .intro .description {
                max-width: 500px;
                font-size: 1rem;
            }

            .social-btn {
                width: 56px;
                height: 56px;
                font-size: 1.3rem;
            }

            .service-card {
                flex: 0 0 45%;
                padding: 2rem 1.5rem;
            }

            .section-title, .services-title, .appointment-title {
                font-size: 2rem;
            }
        }

        /* Desktop optimizations */
        @media (min-width: 768px) {
            .hero-wrap {
                padding: 2rem 0 1rem;
            }

            .hero-bg {
                height: 280px;
            }

            .profile-photo {
                width: 160px;
                height: 160px;
                margin-top: -80px;
            }

            .intro {
                padding: 2.5rem 1rem 2rem;
            }

            .intro h1 {
                font-size: 3rem;
            }

            .contact-section {
                padding: 3.5rem 0;
            }

            .services-wrap {
                padding: 4rem 0;
            }

            .service-card {
                flex: 0 0 30%;
            }

            .appointment-wrap {
                padding: 3.5rem 1rem;
            }

            .info-card {
                padding: 1.5rem;
            }
        }

        /* Large desktop optimizations */
        @media (min-width: 992px) {
            .hero-bg {
                height: 320px;
            }

            .profile-photo {
                width: 180px;
                height: 180px;
                margin-top: -90px;
            }

            .intro h1 {
                font-size: 3.5rem;
            }
        }

        /* Print styles */
        @media print {
            .social-row,
            .appointment-wrap {
                display: none;
            }

            body {
                background: white;
                color: black;
            }
        }

        /* Reduced motion */
        @media (prefers-reduced-motion: reduce) {
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }

            html {
                scroll-behavior: auto;
            }
        }
    </style>
</head>

<body>
    <main role="main">
        <!-- HERO SECTION -->
        <header class="hero-wrap">

            <div class="hero-bg" aria-hidden="true">
                <svg class="wave" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 150" preserveAspectRatio="none">
                    <path d="M0,80 C360,160 1080,0 1440,80 L1440,150 L0,150 Z"
                        fill="linear-gradient(180deg, #000000 0%, #C50202 90%, #000000 100%)"></path>
                </svg>
            </div>

            <div class="profile-photo" role="img" aria-label="MEHUL PATEL profile photo">
                <img src="images/founder.avif" alt="MEHUL PATEL - Professional Hair Dresser" loading="lazy">
            </div>

            <div class="intro">
                <h1>MEHUL PATEL</h1>
                <div class="subtitle">CEO &amp; Founder – Shivansh Infosys
                </div>
                <p class="description">
                    Founded in April 2007, Shivansh Infosys began with a vision to deliver exceptional service to our
                    clients. Our commitment to excellence has driven our growth and success in the industry.
                </p>

                <div class="company-links">
                    <a href="https://shivanshinfosys.in" target="_blank" class="company-link d-flex align-items-center">
                        <span><i class="fas fa-globe me-1"></i></span> <span>Shivansh Infosys</span>
                    </a>
                </div>

                <nav class="social-row" aria-label="Social media links">
                    <a href="https://bitly.cx/rNEH4" class="social-btn" title="Contact via WhatsApp"
                        aria-label="Youtube">
                        <i class="fab fa-youtube" aria-hidden="true"></i>
                    </a>
                    <a href="https://api.whatsapp.com/send/?phone=8141703007&text&type=phone_number&app_absent=0"
                        class="social-btn" title="Contact via WhatsApp" aria-label="WhatsApp">
                        <i class="fab fa-whatsapp" aria-hidden="true"></i>
                    </a>
                    <a href="https://www.linkedin.com/in/mehul-patel-0a7973355?utm_source=share&utm_campaign=share_via&utm_content=profile&utm_medium=android_app"
                        class="social-btn" title="Connect on LinkedIn" aria-label="LinkedIn">
                        <i class="fab fa-linkedin-in" aria-hidden="true"></i>
                    </a>
                    <a href="https://www.instagram.com/shivansh_infosys?igsh=MW5vdjB5YTY2Y2pmdw==" class="social-btn"
                        title="Follow on Instagram" aria-label="Instagram">
                        <i class="fab fa-instagram" aria-hidden="true"></i>
                    </a>
                    <a href="https://www.facebook.com/profile.php?id=61573592214242&mibextid=rS40aB7S9Ucbxw6v"
                        class="social-btn" title="Follow on Facebook" aria-label="Facebook">
                        <i class="fab fa-facebook-f" aria-hidden="true"></i>
                    </a>
                </nav>
            </div>
        </header>

        <!-- CONTACT SECTION -->
        <section class="contact-section">
            <div class="container">
                <h2 class="section-title">Contact Information</h2>

                <div class="row g-4">
                    <div class="col-12 col-md-6">
                        <a class="info-link d-block" href="mailto:shivanshinfosys@gmail.com" aria-label="Email Me">
                            <article class="info-card">
                                <div class="info-icon" aria-hidden="true">
                                    <i class="far fa-envelope"></i>
                                </div>
                                <div class="info-text">
                                    <small>E-mail address</small>
                                    <div class="value">shivanshinfosys@gmail.com</div>
                                </div>
                            </article>
                        </a>
                    </div>

                    <div class="col-12 col-md-6">
                        <a class="info-link d-block" href="tel:+918141703007" aria-label="Call Me">
                            <article class="info-card">
                                <div class="info-icon" aria-hidden="true">
                                    <i class="fas fa-phone"></i>
                                </div>
                                <div class="info-text">
                                    <small>Mobile Number</small>
                                    <div class="value">+91 8141703007</div>
                                </div>
                            </article>
                        </a>
                    </div>

                    <div class="col-12 col-md-6">
                        <a class="info-link d-block" href="https://maps.app.goo.gl/hX9uJ7kRkJgqovkT8" target="_blank"
                            rel="noopener" aria-label="Open location in maps">
                            <article class="info-card">
                                <div class="info-icon" aria-hidden="true">
                                    <i class="fas fa-location-dot"></i>
                                </div>
                                <div class="info-text">
                                    <small>Location</small>
                                    <div class="value">
                                        214, 215 Soham Arcade, Green city, Adajan, Surat, Gujarat
                                    </div>
                                </div>
                            </article>
                        </a>
                    </div>
                    <div class="col-12 col-md-6">
                        <div class="map-container">
                            <iframe
                                src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d4577.419796498114!2d72.7712071!3d21.189703700000003!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3be04db2480c89d9%3A0x41a9df463ca6ff0d!2sSoham%20Arcade!5e1!3m2!1sen!2sin!4v1755949082557!5m2!1sen!2sin"
                                width="100%" height="100%" style="border:0;" allowfullscreen=""
                                loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- SERVICES SECTION -->
        <section class="services-wrap">
            <div class="container">
                <h3 class="services-title">Our Services</h3>

                <div class="services-scroll-container">
                    <div class="services-scroll" id="services-scroll">
                        <article class="service-card">
                            <div class="service-icon" aria-hidden="true">
                                <i class="fas fa-calculator"></i>
                            </div>
                            <h5>Tally Implementation</h5>
                            <p>Complete Tally ERP setup and customization for your business needs</p>
                        </article>

                        <article class="service-card">
                            <div class="service-icon" aria-hidden="true">
                                <i class="fas fa-code"></i>
                            </div>
                            <h5>Custom TDL</h5>
                            <p>Tailored Tally Definition Language solutions for unique requirements</p>
                        </article>

                        <article class="service-card">
                            <div class="service-icon" aria-hidden="true">
                                <i class="fas fa-file-invoice"></i>
                            </div>
                            <h5>E-Invoice Setup</h5>
                            <p>Seamless integration with GST e-invoice portal and compliance.</p>
                        </article>

                        <article class="service-card">
                            <div class="service-icon" aria-hidden="true">
                                <i class="fas fa-truck"></i>
                            </div>
                            <h5>E-Way Bill Setup</h5>
                            <p>Automated e-way bill generation and management system</p>
                        </article>

                        <article class="service-card">
                            <div class="service-icon" aria-hidden="true">
                                <i class="fas fa-headset"></i>
                            </div>
                            <h5>Customer Support</h5>
                            <p>Round-the-clock customer support and maintenance services</p>
                        </article>
                    </div>

                    <!-- <div class="scroll-indicator" id="scroll-indicator">
                        <div class="scroll-dot active"></div>
                        <div class="scroll-dot"></div>
                        <div class="scroll-dot"></div>
                        <div class="scroll-dot"></div>
                        <div class="scroll-dot"></div>
                    </div> -->
                </div>
            </div>
        </section>

        <!-- FOOTER -->
        <footer class="footer">
            <div class="container">
                <p>&copy; 2025 Mehul Patel | Shivansh Infosys</p>
                <p style="font-size: 12px; opacity: 0.7;">
                    Empowering businesses with technology since 2007
                </p>
            </div>
        </footer>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>

    <script>
        // Initialize app
        document.addEventListener('DOMContentLoaded', function () {
            initializeApp();
        });

        function initializeApp() {
            // Set current year
            document.getElementById('current-year').textContent = new Date().getFullYear();

            // Initialize date input
            initializeDateInput();

            // Add smooth scrolling
            addSmoothScrolling();

            // Add touch/interaction feedback
            addInteractionFeedback();

            // Add loading states
            addLoadingStates();

            // Initialize services scroll
            initServicesScroll();

            // Add entrance animations
            addEntranceAnimations();
        }

        function initServicesScroll() {
            const scrollContainer = document.getElementById('services-scroll');
            const dots = document.querySelectorAll('.scroll-dot');

            if (!scrollContainer) return;

            // Update dots based on scroll position
            scrollContainer.addEventListener('scroll', function () {
                const scrollPos = scrollContainer.scrollLeft;
                const cardWidth = scrollContainer.querySelector('.service-card').offsetWidth + 16; // width + gap
                const activeIndex = Math.round(scrollPos / cardWidth);

                dots.forEach((dot, index) => {
                    if (index === activeIndex) {
                        dot.classList.add('active');
                    } else {
                        dot.classList.remove('active');
                    }
                });
            });

            // Click on dots to scroll to specific service
            dots.forEach((dot, index) => {
                dot.addEventListener('click', function () {
                    const cardWidth = scrollContainer.querySelector('.service-card').offsetWidth + 16;
                    scrollContainer.scrollTo({
                        left: index * cardWidth,
                        behavior: 'smooth'
                    });
                });
            });
        }

        function initializeDateInput() {
            const dateInput = document.getElementById('appointment-date');
            if (!dateInput) return;

            // Set minimum date to today
            const today = new Date();
            const yyyy = today.getFullYear();
            const mm = String(today.getMonth() + 1).padStart(2, '0');
            const dd = String(today.getDate()).padStart(2, '0');
            const todayString = `${yyyy}-${mm}-${dd}`;

            dateInput.min = todayString;

            // Set maximum date to 3 months from now
            const maxDate = new Date();
            maxDate.setMonth(maxDate.getMonth() + 3);
            const maxDateString = `${maxDate.getFullYear()}-${String(maxDate.getMonth() + 1).padStart(2, '0')}-${String(maxDate.getDate()).padStart(2, '0')}`;
            dateInput.max = maxDateString;
        }

        function addSmoothScrolling() {
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                });
            });
        }

        function addInteractionFeedback() {
            // Add touch feedback for mobile
            const interactiveElements = document.querySelectorAll('.social-btn, .info-card, .service-card, .book-btn, .company-link');

            interactiveElements.forEach(element => {
                // Touch start
                element.addEventListener('touchstart', function () {
                    this.style.transform = this.style.transform.replace('scale(1)', 'scale(0.98)');
                }, { passive: true });

                // Touch end
                element.addEventListener('touchend', function () {
                    setTimeout(() => {
                        this.style.transform = this.style.transform.replace('scale(0.98)', 'scale(1)');
                    }, 150);
                }, { passive: true });
            });

            // Add ripple effect to buttons
            const buttons = document.querySelectorAll('.book-btn, .social-btn, .company-link');
            buttons.forEach(button => {
                button.addEventListener('click', createRipple);
            });
        }

        function createRipple(event) {
            const button = event.currentTarget;
            const ripple = document.createElement('span');
            const rect = button.getBoundingClientRect();
            const size = Math.max(rect.width, rect.height);
            const x = event.clientX - rect.left - size / 2;
            const y = event.clientY - rect.top - size / 2;

            ripple.style.cssText = `
                position: absolute;
                width: ${size}px;
                height: ${size}px;
                left: ${x}px;
                top: ${y}px;
                background: rgba(255, 255, 255, 0.3);
                border-radius: 50%;
                transform: scale(0);
                animation: ripple 0.6s ease-out;
                pointer-events: none;
            `;

            // Add ripple animation
            const style = document.createElement('style');
            if (!document.querySelector('#ripple-animation')) {
                style.id = 'ripple-animation';
                style.textContent = `
                    @keyframes ripple {
                        to {
                            transform: scale(4);
                            opacity: 0;
                        }
                    }
                `;
                document.head.appendChild(style);
            }

            button.style.position = 'relative';
            button.style.overflow = 'hidden';
            button.appendChild(ripple);

            setTimeout(() => {
                ripple.remove();
            }, 600);
        }

        function addLoadingStates() {
            // Add loading states to images
            const images = document.querySelectorAll('img[loading="lazy"]');
            images.forEach(img => {
                img.addEventListener('load', function () {
                    this.style.opacity = '1';
                });
                img.style.opacity = '0';
                img.style.transition = 'opacity 0.3s ease';
            });
        }

        function addEntranceAnimations() {
            // Animate elements on scroll
            const animatedElements = document.querySelectorAll('.info-card, .service-card, .profile-photo, .section-title, .services-title, .appointment-title');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('animate-fade-in-up');
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.1 });
            
            animatedElements.forEach(element => {
                observer.observe(element);
            });
        }

        // Error handling
        window.addEventListener('error', function (event) {
            console.error('Application Error:', event.error);
        });

        // Accessibility enhancements
        function enhanceAccessibility() {
            // Skip to main content link
            const skipLink = document.createElement('a');
            skipLink.href = '#main';
            skipLink.textContent = 'Skip to main content';
            skipLink.className = 'visually-hidden';
            skipLink.style.cssText = `
                position: absolute;
                left: -10000px;
                top: auto;
                width: 1px;
                height: 1px;
                overflow: hidden;
            `;
            skipLink.addEventListener('focus', function () {
                this.style.cssText = `
                    position: absolute;
                    left: 6px;
                    top: 7px;
                    z-index: 999999;
                    padding: 8px 16px;
                    background: var(--gold-primary);
                    color: var(--teal-primary);
                    text-decoration: none;
                    border-radius: 4px;
                    font-weight: 600;
                `;
            });
            skipLink.addEventListener('blur', function () {
                this.style.cssText = `
                    position: absolute;
                    left: -10000px;
                    top: auto;
                    width: 1px;
                    height: 1px;
                    overflow: hidden;
                `;
            });
            document.body.insertBefore(skipLink, document.body.firstChild);

            // Add main landmark
            document.querySelector('main').id = 'main';

            // Announce dynamic content changes
            const announcer = document.createElement('div');
            announcer.setAttribute('aria-live', 'polite');
            announcer.setAttribute('aria-atomic', 'true');
            announcer.className = 'visually-hidden';
            announcer.id = 'announcer';
            document.body.appendChild(announcer);
        }

        // Initialize accessibility enhancements
        enhanceAccessibility();
    </script>
</body>

</html>